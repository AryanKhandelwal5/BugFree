# Contributing

Create focused branches and use Conventional Commits. Before opening a pull request, run `make check`, keep coverage at or above 90%, update OpenAPI-visible contracts and documentation, and include tests for failure paths. Do not commit `.env`, credentials, generated coverage, or local databases.

